(
~mh[\conductor] = (
	// Tuning keys
	freq: 440,
	diapason: 440,
	scale: \minor,
	// Rythm keys
	bpm: 60,
	currentSection: 0,
	maxSection: 0,
	currentCycle: 0,
	maxCycle: inf,
	currentBeat: 0,
	maxBeat: 4,
	currentSub: 0,
	maxSub: 4,
);
)